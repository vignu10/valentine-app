# Public Assets

This folder is for static assets.

## Character Images

You can optionally add character images here:
- `cody.png` - Cody character image
- `may.png` - May character image

The app uses emoji characters by default, so these are optional.
